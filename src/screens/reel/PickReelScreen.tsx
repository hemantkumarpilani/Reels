import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const PickReelScreen = () => {
  return (
    <View>
      <Text>PickReelScreen</Text>
    </View>
  );
};

export default PickReelScreen;

const styles = StyleSheet.create({});
